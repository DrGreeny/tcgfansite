import FAQList from "../../components/FaqList";

export default function gameRulesFaq() {
  return (
    <div
      className=""
      style={{
        minHeight: `calc(100vh - 64px)`,
      }}
    >
      <FAQList />
    </div>
  );
}
